[workspace]
members = [
    "moltendb-core",
    "moltendb-auth",
    "moltendb-wasm",
    "moltendb-server",
]
resolver = "2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"
